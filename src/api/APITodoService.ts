// import { api } from "./APIServices";

// export const getTodos = (token: string) => api.getTodos(token);
// export const getTodoDetails = (id: string, token: string) => api.getTodoDetails(id, token);
// export const createTodo = (todo: any, token: string) => api.createTodo(todo, token);
// export const updateTodo = (id: string, todo: any, token: string) => api.updateTodo(id, todo, token);
// export const deleteTodo = (id: string, token: string) => api.deleteTodo(id, token);
// export const cloneTodo = (id: string, token: string) => api.cloneTodo(id, token);
// export const reorderTodos = (order: string[], token: string) => api.reorderTodos(order, token);

